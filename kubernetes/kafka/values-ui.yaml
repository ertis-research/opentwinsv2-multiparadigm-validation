yamlApplicationConfig:
  kafka:
    clusters:
      - name: OpenTwinsKafka
        bootstrapServers: >
          otv2-mpm-kafka.opentwinsv2-test2.svc.cluster.local:9092
  auth:
    type: disabled
  management:
    health:
      ldap:
        enabled: false
service:
  type: NodePort
