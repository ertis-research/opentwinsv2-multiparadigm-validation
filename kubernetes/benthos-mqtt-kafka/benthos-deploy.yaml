apiVersion: apps/v1
kind: Deployment
metadata:
  name: otv2-mpm-benthos-mqtt-kafka
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otv2-mpm-benthos-mqtt-kafka
  template:
    metadata:
      labels:
        app: otv2-mpm-benthos-mqtt-kafka
    spec:
      containers:
        - name: benthos-mqtt-kafka
          image: jeffail/benthos:latest
          args: ["-c", "/config/benthos.yaml"]
          volumeMounts:
            - name: config
              mountPath: /config
      volumes:
        - name: config
          configMap:
            name: benthos-mqtt-kafka-config